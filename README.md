# api-impots

```Bash
$ ~ cd api-impots
$ ~ npm install
```
Run server (developpement)
```Bash 
$ ~ npm run start:dev
```

Run server (production)
```Bash 
$ ~ npm run start
```
`Server is running on http://localhost:3005/`
